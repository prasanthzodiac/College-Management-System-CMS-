# =============================================================================
# VERCEL ONLY — Project → Settings → Environment Variables → Import or add each
# Do NOT add VITE_* variables to the Render backend service.
# Rebuild / redeploy after any change (Vite bakes these at build time).
# =============================================================================

VITE_API_BASE_URL=https://your-api.onrender.com

VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_APP_ID=
